# Copy this file to ".env" and fill values as needed.
# DO NOT commit ".env" to GitHub.

# Flask environment
FLASK_ENV=development
# SECRET_KEY is required for session cookies, CSRF, etc.
# Generate your own with: python -c "import secrets; print(secrets.token_hex(16))"
SECRET_KEY=change-me

# Database location (relative to project root by default)
DB_PATH=db/gitgrub.sqlite3

# Bootstrap admin user (created when running seed.py)
# Change these before deploying anywhere real
ADMIN_EMAIL=admin@example.com

# Leave blank to auto-generate a secure password at seed time
# Password will be printed to the console
# ADMIN_PASSWORD=
